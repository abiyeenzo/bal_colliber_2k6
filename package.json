{
  "name": "bal-promo-libermann-fullstack",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "start": "node server/index.js",
    "build": "npm run install-client && npm run build-client"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5"
  }
}